using UnityEngine;

public class EnemyFRONT : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    private void Awake()
    {
        // spawn logic - spawns randomly on right
    }

    // Update is called once per frame
    void Update()
    {
        //slowly moves to left side of screen

        //move on y axis towards player, or randomly possibly.

        //if past screen, destroy.

        //shoot Enemyprojectile forward aimlessly every so often

    }

    //check for if shot (award points)

    //check for if physically touched (dmg plr then destroy)

    //check for asteroid hit
}
