using UnityEngine;

public class BULLETenemy : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //start moving towards left
    }

    // if touches asteroid, damage it (this might be in asteroid script so check for that)

    // if touches player, take a life

}
